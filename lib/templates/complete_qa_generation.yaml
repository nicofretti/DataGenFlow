name: Complete QA Generation
description: Full pipeline with LLM generation and validation
blocks:
  - type: TextGenerator
    config:
      temperature: 0.7
      max_tokens: 2048
      system_prompt: ""
      user_prompt: ""

  - type: ValidatorBlock
    config:
      min_length: 20
      max_length: 5000
