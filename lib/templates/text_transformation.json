{
  "name": "Text Transformation Pipeline",
  "description": "Transform and clean text using multiple operations",
  "blocks": [
    {
      "type": "TransformerBlock",
      "config": {
        "operation": "strip"
      }
    },
    {
      "type": "TransformerBlock",
      "config": {
        "operation": "lowercase"
      }
    },
    {
      "type": "TransformerBlock",
      "config": {
        "operation": "trim"
      }
    }
  ]
}
